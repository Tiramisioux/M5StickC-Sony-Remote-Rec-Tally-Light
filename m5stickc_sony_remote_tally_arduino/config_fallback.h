#pragma once

// Compile-safe fallback used when config.h is missing.
// The sketch will build and boot, but it will stop at an EDIT CFG state.
#define M5_SONY_WIFI_SSID "SET_ME"
#define M5_SONY_WIFI_PASSWORD "SET_ME"
